### BusTime
This bot could be used as a parser of data and then it sends it to the user who asked for this data. To use it just get the code and requirements.txt and then 
**pip install -r requirements.txt** and create .env file and put your own token into it. Also add url to parse to the .env file , **change classes in the **async def getting_bus_time()****
## Run ad.py one time  before py.py to create database
